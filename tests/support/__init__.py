"""Builders and fakes shared by more than one test module.

Importable as ``tests.support`` because conftest puts the project root on ``sys.path``. Anything
used by a single module stays in that module.
"""
